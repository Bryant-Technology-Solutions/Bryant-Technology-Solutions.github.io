source "https://rubygems.org"

gem "github-pages", group: :jekyll_plugins
# gem "jekyll", "~> X.X.X" # Uncomment this line and specify a version if not using the github-pages gem

# If you have any additional supported plugins, put them here!
group :jekyll_plugins do
  # gem "jekyll-feed", "~> X.X"
  # gem "jekyll-sitemap"
end

# Windows and JRuby does not include zoneinfo files, so bundle the tzinfo-data gem
# and associated library.
if Gem.win_platform?
  gem "tzinfo-data"
  gem "wdm", "~> 0.1.0"
end
